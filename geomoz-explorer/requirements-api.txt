# ── GeoMoz API — production dependencies ─────────────────────────────────────
# Streamlit & streamlit-folium excluded: they are not used by the FastAPI
# backend and add ~200 MB to the Docker image.

fastapi>=0.100.0
uvicorn>=0.23.0
geopandas>=0.14.0
shapely>=2.0.0
geomoz
pandas>=2.0.0
numpy>=1.26.0
earthengine-api>=1.7.0
httpx>=0.25.0
python-multipart>=0.0.28

# Security patches
idna>=3.16           # GHSA-65pc-fj4g-8rjx
starlette>=1.0.1     # PYSEC-2026-161
firebase-admin>=6.5.0
cryptography>=42.0.0
google-genai>=0.1.1
